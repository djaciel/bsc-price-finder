const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV3');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'APESWAP';
const amount = '1' + '000000000000000000';
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.cake, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.txl, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.cos, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bunny, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.alice, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.for, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bux, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.nuls, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.belt, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ramp, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bfi, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.dexe, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bel, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.tpt, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.watch, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.xmark, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bmxx, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.iotx, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bor, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bopen, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.dodo, true, false);
PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.swingby,
  true,
  false
);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bry, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.zee, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.swgb, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.swg, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.sfp, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.lina, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.lit, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.hget, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bdo, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.egld, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ust, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.wsote, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.front, true, false);
PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.helmet,
  true,
  false
);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.btcst, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bscx, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ten, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.balbt, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.asr, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.atm, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.og, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.reef, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ditto, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.juv, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.psg, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.vai, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.blink, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.unfi, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.twt, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.hard, true, false);
PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.broobee,
  true,
  false
);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.stax, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.nar, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.nya, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ctk, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.inj, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.sxp, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.alpha, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.xvs, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.sushi, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.comp, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.syrup, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bifi, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.dusk, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.busd, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.eth, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.beth, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.mamzn, true, false);

PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.mgoogl,
  true,
  false
);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.mnflx, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.mtsla, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ltc, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.usdc, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.dai, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ada, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.band, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.dot, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.eos, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.link, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.usdt, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.btcb, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.xrp, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.atom, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.yfii, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.xtz, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bch, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.yfi, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.uni, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.fil, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bake, true, false);
PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.burger,
  true,
  false
);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bdigg, true, false);
PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.bbadger,
  true,
  false
);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.trade, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.pnt, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.mir, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.pbtc, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.lto, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.pcws, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.zil, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.lien, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.swth, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.dft, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.gum, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.dego, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.nrv, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.easy, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.oddz, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.hoo, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.apys, true, false);
PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.bondly,
  true,
  false
);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.tko, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.itam, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.arpa, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.eps, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.jgn, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.tlm, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.perl, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.alpa, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.hzn, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.suter, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.cgg, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.mix, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.hakka, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.xed, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.pibtc, true, false);
PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.alpaca,
  true,
  false
);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.dfd, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.lmt, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.btt, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.trx, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.win, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.mcoin, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.math, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.kun, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.qsd, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.hyfi, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.oin, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.doge, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.fine, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.one, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.pmon, true, false);
PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.hotcross,
  true,
  false
);
PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.pidoge,
  true,
  false
);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.btr, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ubxt, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.wmass, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.rfox, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.xend, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.cyc, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.chr, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.kalm, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.deri, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.well, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.popen, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ez, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.vrt, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.tusd, true, false);

PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.mtrg, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ktn, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.qkc, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.bcfx, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.mx, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.ata, true, false);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.mbox, true, false);
PriceFinder.comparePrices(
  mainRouterName,
  amount,
  tokens.busd,
  tokens.boring,
  true,
  false
);
PriceFinder.comparePrices(mainRouterName, amount, tokens.busd, tokens.marsh, true, false);
