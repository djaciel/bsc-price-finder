const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.ADA, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.ALICE, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.ALPACA, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.ALPHA, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.AQUAGOAT, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.ASS, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BAND, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BELT, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BIFI, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BOG, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BONFIRE, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BP, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BSCPAD, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BSCS, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BTCB, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BTCST, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BTT, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BUNNY, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.BUSD, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.CAKE, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.CATE, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.CLU, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.COMP, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.CROSS, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.CUMMIES, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.DAI, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.DCH, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.DFD, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.DODO, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.DOGE, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.DOGGY, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.DOP, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.DOT, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.ElonGate, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.EOS, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.EPS, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.ETH, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.FEG, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.FINE, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.FTS, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.GHOSTFACE, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.GMR, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.HODL, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.HOTCROSS, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.INJ, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.IOTX, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.JAGUAR, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.JGN, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.KABOSU, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.LINA, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.LINK, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.LIZ, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.LOWB, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.LTC, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.MBOX, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.MEPAD, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.NAFT, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.NFTART, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.NRV, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.PANTHER, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.PARA, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.pBTC, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.PIG, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.RAMP, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.REEF, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SAFEMARS, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SAFEMOON, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SAFEPLUTO, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SAT, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SAVE, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SFP, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SFUND, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SMARS, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SUSHI, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SWAMP, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.SXP, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.tBTC, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.TKO, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.TLM, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.TPT, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.TRX, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.TWT, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.ULTRA, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.UNCL, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.UNI, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.USDC, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.USDT, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.UST, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.VAI, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.VRT, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.WAULTx, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.WEX, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.WIN, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.WOOP, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.WSB, true, true);

const PriceFinder = require('./PriceFinder');
const tokens = require('./tokensV2');
console.log(`Deadline: ${Math.floor(Date.now() / 1000) + 60 * 20}`);
const mainRouterName = 'PANCAKESWAP';
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.XDOGE, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.XRP, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.XVS, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.YUMMY, true, true);
PriceFinder.comparePrices(mainRouterName, tokens.WBNB, tokens.ZIL, true, true);
